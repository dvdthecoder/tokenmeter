# Tokenmeter Status

Shows the current state of the tokenmeter daemon and recent token usage.

```bash
tokenmeter status
tokenmeter query --last 24h --format table
```

Reports: daemon health, active sinks, events in the last 24h, total tokens and estimated cost.
